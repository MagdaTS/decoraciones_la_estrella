# Direct dependencies for compress_catalogs.py
# Pinned to the versions requested by the user.
pypdfium2==4.30.1
img2pdf==0.5.1
Pillow==11.0.0
tqdm==4.67.1

# Transitive dependency (tqdm on Windows pulls colorama).
colorama==0.4.6
